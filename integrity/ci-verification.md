# Repository Integrity Verification

This marker exists solely to trigger a clean pull-request validation run after the integrity remediation published to `main`.

The validation target is the current `main` tree, including JSON syntax, Python syntax, local references, event schemas, ontology vocabulary, and runtime dependency checks.
